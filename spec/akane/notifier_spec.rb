require "spec_helper"

describe Akane::Notifier do
  it "has a version number" do
    expect(Akane::Notifier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
